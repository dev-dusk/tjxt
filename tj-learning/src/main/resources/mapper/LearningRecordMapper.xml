<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianji.learning.mapper.LearningRecordMapper">

    <select id="queryLearningRecordCount" resultType="com.tianji.api.dto.IdAndNumDTO">
        select lesson_id as id, count(1) as num
        from learning_record
        where finished = '1' and user_id = #{user}
        and finish_time between #{start} and #{end}
        group by lesson_id
    </select>
    <select id="queryLearningRecordList" resultType="com.tianji.learning.domain.po.LearningRecord">
        select * from learning_record
        where user_id = #{user}
        and lesson_id in
        <foreach item="item" collection="lessonIds" separator="," open="(" close=")" index="index">
            #{item}
        </foreach>
        and finish_time between #{start} and #{end}
        and finished = '1'
    </select>
</mapper>
